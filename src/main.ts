import { Game } from "./Game";

let controls = {
    up: 'ArrowUp',
    right: 'ArrowRight',
    down: 'ArrowDown',
    left: 'ArrowLeft',
    shoot: 'z',
    hitboxes: 'h'
}
let game = new Game(controls);